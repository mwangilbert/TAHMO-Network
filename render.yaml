services:
  - type: web
    name: tahmo-network-portal
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python tahmo_server.py
    envVars:
      - key: DASH_USER
        sync: false
      - key: DASH_PASS
        sync: false
